# Environment Configuration Template for Congressional MCP Server
# =============================================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.development, .env.production, or .env.staging
# 2. For local development: cp .env.template .env.development
# 3. For production: Use .env.production or set via 'heroku config:set'
# 
# ENVIRONMENT DETECTION:
# - Development: Automatic if no PORT environment variable
# - Production: Automatic if PORT is set (Heroku)
# - Manual: Set CONGRESS_API_ENV=development|production|staging

# Required Variables
# ------------------

# API Configuration
CONGRESS_API_KEY=your_api_key_here  # Required: Your Congress.gov API key

# Authentication Configuration
LAWGIVER_JWT_SECRET=your_jwt_secret_here  # Required for JWT authentication
LAWGIVER_API_KEYS=free:user1:key1,pro:user2:key2,enterprise:user3:key3  # Comma-separated list of valid API keys
ENABLE_AUTH=false  # Set to true to enable authentication

# Stripe Configuration
# NOTE: Different webhook secrets for development vs production
# - Development: Use Stripe CLI secret (whsec_...) from 'stripe listen'
# - Production: Use webhook endpoint secret from Stripe Dashboard
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here  # Required for Stripe webhook verification
ENABLE_STRIPE=false  # Set to true to enable Stripe integration

# Supabase Configuration
SUPABASE_URL=your_supabase_project_url_here  # Required: Your Supabase project URL (https://xxx.supabase.co)
SUPABASE_ANON_KEY=your_supabase_anon_key_here  # Required: Supabase anonymous key (for client operations)
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here  # Required: Supabase service role key (for admin operations)
ENABLE_DATABASE=true  # Set to true to enable database features

# Admin Configuration
ADMIN_API_KEY=your_admin_api_key_here  # Required for admin API access
ENABLE_KEY_MANAGEMENT=false  # Set to true to enable key management API

# Optional Variables with Defaults
# -------------------------------

# API Configuration
CONGRESS_API_BASE_URL=https://api.congress.gov/v3  # Base URL for the Congress.gov API

# Environment (auto-detected if not set)
CONGRESS_API_ENV=development  # development, staging, or production

# Logging Configuration
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Caching Configuration
ENABLE_CACHING=true  # Enable or disable caching (true/false)
CACHE_TIMEOUT=300  # Cache timeout in seconds (5 minutes)

# Heroku-Specific Variables
# ------------------------
# PORT=8000  # Automatically set by Heroku, do not set manually
# WEB_CONCURRENCY=1  # Number of Gunicorn workers, defaults to 1
